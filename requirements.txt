# SECOM TDA Project - Requirements
# =================================

# Core dependencies
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0

# TDA (Topological Data Analysis)
ripser>=0.6.0
persim>=0.3.0

# Visualization
matplotlib>=3.4.0
seaborn>=0.11.0

# PDF generation (optional - for diagram_sidebyside.pdf)
weasyprint>=52.0

# Note: WeasyPrint requires additional system dependencies.
# On Windows, you may need to install GTK3.
# If WeasyPrint fails to install, visualization will still work
# but PDF output will be skipped.
